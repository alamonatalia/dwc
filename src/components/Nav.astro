---
interface Link {
    href: string;
    text: string;
}

const links = [
	// { text: "Inicio", href: "#" },
    { text: "Nosotros", href: "#company" },
    { text: "Proyectos", href: "#projects" },
    { text: "Servicios", href: "#services" },
];
const menuId = "mobile-menu";
---
<nav
     id={menuId}
     class="relative hidden md:block">
     <ul 
        class="flex lg:flex-row lg:bg-transparent w-full lg:w-auto z-20 lg:z-auto p-4 lg:p-0 gap-4 lg:gap-0 transition-all duration-300">
        {
            links.map((link: Link) => (
                <li>
                    <a
                        href={link.href}
                        class=" font-sharp_grotesk_light text-sm text-[var(--brand-gray-300)] px-3 py-2 mx-1 block rounded-md transition duration-200 hover:bg-white/20 hover:text-[var(--brand-gray-50)]"
                    >
                        {link.text}
                    </a>
                </li>
            ))
        }
    </ul>
</nav>